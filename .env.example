# Copy to .env and set before exposing the router on a public host.
# Clients send this value as: Authorization: Bearer <key>
# or header: X-API-Key: <key>

ROUTER_API_KEY=change-me-before-going-public
